if [ -z $1 ]; then
  echo ERROR Specify repository url
  exit 1
fi

docker build -t nginx-site .
TAG=$1latest
TAG=$(echo $TAG  sed 's^.')
docker tag nginx-sitelatest $TAG
LOGIN=$(aws ecr get-login --region eu-central-1)
$LOGIN
docker push $TAG